function Form() {
  return (
    <div>
      <div className="mt-[10px] rounded bg-white p-4 shadow-lg">
        {/* <div className="flex items-center gap-3 bg-white">
          <h2
            onClick={() => toggleKey(true)}
            className={`cursor-pointer bg-white font-bold capitalize ${
              key ? "text-blue-500" : "text-stone-700"
            }  transition-all  duration-300 ease-in-out`}
          >
            Phrase
          </h2>
          <h2
            onClick={() => toggleKey(false)}
            className={`cursor-pointer bg-white font-bold capitalize ${
              !key ? "text-blue-500" : "text-stone-700"
            }  transition-all  duration-300 ease-in-out`}
          >
            private key
          </h2>
        </div>
        <div className="bg-white">
          <Form />
        </div> */}
        HIIIIII
      </div>
    </div>
  );
}

export default Form;
